{
  "name": "timestamp-microservice",
  "version": "0.0.1",
  "main": "dist/server.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon server.ts",
    "compile": "ts-node server.ts",
    "build": "tsc -p ./",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.9",
    "@types/express": "^4.17.11",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  }
}
